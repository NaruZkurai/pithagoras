node_modules/
dist/
data/
data-1338/
.venv/
.env
.env.local
*.tsbuildinfo
.DS_Store

# Model weights — never commit multi-GB GGUFs to git.
*.gguf
Base Models/

# The copied llama-direct-token-input fork is its own repo (pasted in, not a
# submodule). prime-agent IS a tracked submodule and must NOT be ignored.
gitrepos/llama-direct-token-input/

# VitePress build output and cache
docs/.vitepress/dist
docs/.vitepress/cache
data/runner-node_modules/

# Self-augmentation generated output (augment-500mb.mjs runtime artifacts)
scripts/output/

# topk-compare / augmentation runtime outputs
data/augment/
output/

# Native-ternary model segments + exported ternary models — multi-GB,
# regenerable via grow_model_30b.py / prepare-model-ram.py from source GGUFs.
config/moe/model/**/*.tern
config/moe/model/**/model.bin
config/moe/model/**/ternary-*/

# Python bytecode (export/rebuild/patch models)
__pycache__/
*.pyc

# Generated USDT wallet-tracer risk report (wallet_tracer.py --html output)
scripts/report.html
*.report.html
